# `@liowms/api`

HTTP API (modular monolith). **S0.1:** install module — migrations, install lock, `GET /health` (`installed`), wizard endpoints (WMS-87).

See company pack ADR-003/004 and `04-quality/test-cases.md` (J0, H-4).
