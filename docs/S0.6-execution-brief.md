# LioWMS S0.6 — execution brief (WMS-29)

| Field | Value |
|-------|--------|
| **Story** | [WMS-29](https://app.plane.so/paperclip-company/browse/WMS-29/) |
| **Slice** | S0.6 — transaction-log append-only + saldo projetado + K11 |
| **Baseline** | `main` @ `abf71cd` (pós-S0.5 audit + K10) |
| **Staging** | `docs/STAGING-S01.md` |
| **Planning** | brain `05-delivery/planning-s0.6.md` |
| **Parent** | Paperclip [PAP-165](/PAP/issues/PAP-165) |

## Valor

**Transaction-log** append-only para movimentos de estoque kernel; **saldo = Σ lançamentos** (ADR-005). API sem UPDATE/DELETE de movimentos. UI **K11** read-only + painel de saldo projetado.

## Fora de escopo

- Outbox SMTP real → S0.7 ([WMS-30](https://app.plane.so/paperclip-company/browse/WMS-30/))
- Mestres SKU/lote completos → S1.1+

## Backend ([WMS-100](https://app.plane.so/paperclip-company/browse/WMS-100/))

1. Migration `006_inventory_ledger.sql` — schema `ledger`, `inventory_transactions`, RLS, trigger append-only
2. `POST /api/v1/tenant/inventory-movements` — exige `Idempotency-Key`
3. `GET /api/v1/tenant/inventory-transactions` — filtros lote/documento/planta/período
4. `GET /api/v1/tenant/inventory-balances` — projeção `SUM(quantity_delta)`
5. Tests — J0e-01, idempotência, 405, trigger DB

## Frontend ([WMS-101](https://app.plane.so/paperclip-company/browse/WMS-101/))

**K11** — `/app/t/:tenantId/ledger` — tabela movimentos, saldos, formulário kernel staging.

## QA ([WMS-102](https://app.plane.so/paperclip-company/browse/WMS-102/))

| TC | Neste slice |
|----|-------------|
| **J0e-01** | **Executar** — movimento → log → saldo |
| **J0d-01/02** | Regressão leve audit |
| **H-03** | Regressão imutabilidade |

## Aceite

- `main` com BE+FE
- Staging HTTPS K11
- `docs/STAGING-S01.md` SHA atualizado
