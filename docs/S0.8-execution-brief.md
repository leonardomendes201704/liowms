# S0.8 execution brief — Telemetria + health JSON (WMS-31)

| Field | Value |
|-------|--------|
| **Story** | [WMS-31](https://app.plane.so/paperclip-company/browse/WMS-31/) |
| **Slice** | S0.8 — telemetria agregada + health `h-4.v1` + K12 |
| **Baseline** | `main` pós-S0.7 (`007_notify_outbox_s07`) |
| **Staging** | `docs/STAGING-S01.md` |
| **Planning** | `docs/planning-s0.8.md` · [PAP-172](/PAP/issues/PAP-172) done |
| **Parent** | Paperclip exec child (WMS-31) under [PAP-120](/PAP/issues/PAP-120) |

## Valor

Health JSON estável (`HealthResponseH4`, K12) com migrations/filas/outbox worker coerentes em staging; gancho OTLP por env (sem PII cross-tenant); UI K12 read-only de status agregado.

## Fora de escopo

- Export OTLP produção completo, dashboards externos, alertas paging
- Painéis plataforma S0.9 ([WMS-32](https://app.plane.so/paperclip-company/browse/WMS-32/))
- Mestres / inbound S1.1+

## Backend ([WMS-109](https://app.plane.so/paperclip-company/browse/WMS-109/))

1. Enriquecer `GET /health` — schema `packages/shared/src/health.ts`; `queues.detail` reflete outbox worker pós-S0.7
2. Módulo telemetry — hook OTLP configurável por env; payloads agregados sem lote/cliente identificável cross-tenant (H-3 / J12-03)
3. ADR-004 — sem segredos/PII em logs ou export telemetria

## Frontend ([WMS-110](https://app.plane.so/paperclip-company/browse/WMS-110/))

**K12** — rota tenant read-only — status agregado (health + filas); mock MinIO conforme pack.

## QA ([WMS-111](https://app.plane.so/paperclip-company/browse/WMS-111/)) — child separada após BE+FE

| TC | Neste slice |
|----|-------------|
| **H-02** | **Executar** — `/health` + migrations/queues pós-S0.7 |
| **J12-03** | **Executar** — telemetria sem identificadores cross-tenant |
| **H-04** | Regressão leve — PNG UI MinIO |
| **H-01** | Regressão leve — export/settings sem cleartext |

## Aceite exec

- `main` com BE+FE; staging HTTPS K12
- `docs/STAGING-S01.md` SHA atualizado
- C1 `BRAIN-GRAPH.md` + STATUS na child de execução

## Testes

Integração API existente + extensões health/telemetry; `npm test -w @liowms/api` com `LIOWMS_TEST_PG_ADMIN_DSN` quando aplicável.
