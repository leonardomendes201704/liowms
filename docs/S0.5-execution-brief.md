# LioWMS S0.5 — execution brief (WMS-28)

| Field | Value |
|-------|--------|
| **Story** | [WMS-28](https://app.plane.so/paperclip-company/browse/WMS-28/) |
| **Slice** | S0.5 — audit-log append-only + K10 |
| **Baseline** | `main` @ `10b0262` (pós-S0.4 config store + K9) |
| **Staging** | `docs/STAGING-S01.md` |
| **Planning** | brain `05-delivery/planning-s0.5.md` |
| **Parent** | Paperclip exec (CTO) espelho [PAP-153](/PAP/issues/PAP-153) — **não** abrir até handoff CEO→CTO |

## Valor

Trilha **audit append-only** para mudanças de config (e hooks mínimos admin) com UI **K10** read-only. Sem PATCH/DELETE de eventos (H-2). Completa **TC-GOLD-J0d-01** deferido em S0.4.

## Fora de escopo (não antecipar S0.6+)

- Transaction-log / saldo (K11) → **S0.6** ([WMS-29](https://app.plane.so/paperclip-company/browse/WMS-29/))
- Outbox SMTP envio real → **S0.7**
- Telemetria health pleno → **S0.8**

## Backend ([WMS-97](https://app.plane.so/paperclip-company/browse/WMS-97/))

1. **Migration `005_audit_log.sql`**
   - Schema `audit`; tabela `audit_events` (ou equivalente): `id`, `tenant_id`, `occurred_at`, `actor_user_id`, `action`, `entity_type`, `entity_key`, `before_json`, `after_json` (RLS tenant)
   - Grants: app role **INSERT + SELECT** only; **no** UPDATE/DELETE para role de aplicação
2. **Writer** — chamar de `config` service em PATCH settings (e pontos acordados no slice); mascarar chaves secretas (`ADR-004`)
3. **API** (tenant_admin + RLS):
   - `GET /api/v1/tenant/audit-events` — paginação + filtros `actor`, `entity_type`, `from`/`to`
   - **Sem** rotas mutáveis de audit
4. **Higiene** — logs/erros sem valor claro; testes API imutabilidade + redaction
5. **Tests** — J0d-02 (403/405), writer smoke em PATCH settings

Refs: `architecture.md` (módulo **audit**).

## Frontend ([WMS-98](https://app.plane.so/paperclip-company/browse/WMS-98/))

**K10** — audit-log (ux-spec / gallery K10).

1. Rota tenant admin **Audit log** (`/app/audit` ou subnav de settings).
2. Tabela: data, ator, entidade, ação; painel diff before/after read-only.
3. Filtros ator / período (query params ou UI mínima).
4. Estados: loading / empty / error (K5 patterns).
5. Vitest smoke render + lista mocked API.

## QA ([WMS-99](https://app.plane.so/paperclip-company/browse/WMS-99/))

| TC | Neste slice |
|----|-------------|
| **J0d-01** | **Executar** — alterar K9 → evento visível em K10 |
| **J0d-02** | **Executar** — API recusa mutação audit |
| **H-03** | **Executar** — regressão imutabilidade |
| **H-01** | Regressão — settings sem cleartext |

Evidência em `docs/evidence/` + comentário em issue QA.

## Release (após QA verde)

Push `main`, restage HTTPS per `docs/STAGING-S01.md`, atualizar SHA na doc se necessário.

## Aceite story (CTO)

- `main` com BE+FE merged
- Staging HTTPS com K10 acessível
- Plane WMS-28 + WMS-97/98/99 **Done** com evidência QA
