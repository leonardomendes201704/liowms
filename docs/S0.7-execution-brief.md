# S0.7 execution brief — Outbox SMTP (WMS-30)

**Slice:** S0.7 · **Story:** [WMS-30](https://app.plane.so/paperclip-company/browse/WMS-30/) · **Exec:** [PAP-169](/PAP/issues/PAP-169)

## Entregue (BE + FE)

| Área | Detalhe |
|------|---------|
| Migration `007_notify_outbox_s07` | `status`, `attempts`, `last_error`, `dlq_at`, `tenant_id`, índices worker |
| Módulo `notify` | Worker in-process (`ADR-006`), SMTP via nodemailer, rate limit env |
| API K14 | `GET /api/v1/tenant/notify-outbox`, `POST …/:id/retry` (DLQ/failed only) |
| FE K14 | `/app/t/:tenantId/outbox` — fila ativa + DLQ, payload sanitizado |
| Logs | ADR-004 — sem SMTP password / tokens em logs (`safeLog` + payload summary) |

## Env (runner / staging)

| Variável | Uso |
|----------|-----|
| `LIOWMS_PUBLIC_APP_URL` | Links reset/convite no e-mail (base staging HTTPS) |
| `LIOWMS_NOTIFY_WORKER_INTERVAL_MS` | Default 5000 |
| `LIOWMS_NOTIFY_RATE_LIMIT_PER_MIN` | Default 30 |
| `LIOWMS_NOTIFY_DISABLE_WORKER=1` | Testes API |
| `LIOWMS_SMTP_JSON_TRANSPORT=1` | Testes sem rede real |

## TC-GOLD (QA — WMS-108)

| TC | Verificação |
|----|-------------|
| **J0c-01** | K9 SMTP envelope → convite → e-mail no sink staging (K-4) |
| **J0c-02** | Forgot password → link no e-mail → reset E2E |
| **H-01** | Regressão settings/export sem cleartext |

**Fora:** OTLP S0.8, mestres S1.1, inbound S2.1.

## Testes

Integração: `LIOWMS_TEST_PG_ADMIN_DSN=… npm test -w @liowms/api` (incl. `notify-outbox.test.ts`).
